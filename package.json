{
  "name": "tllester-info",
  "version": "1.0.0",
  "description": "Todd Lanier Lester",
  "main": "index.js",
  "scripts": {
    "dev": "tinacms dev -c \"rm -rf public && hugo server -D --panicOnWarning\"",
    "build": "tinacms build && hugo",
    "build-local": "tinacms build --local --skip-indexing --skip-cloud-checks && hugo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sebastosh/tllester-info.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/sebastosh/tllester-info/issues"
  },
  "homepage": "https://github.com/sebastosh/tllester-info#readme",
  "dependencies": {
    "@tinacms/auth": "^1.1.2",
    "@tinacms/cli": "^2.1.8",
    "next-tinacms-cloudinary": "^23.0.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "tinacms": "^3.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.7.2"
  }
}
